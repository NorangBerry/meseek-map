namespace Meseek.Crawler.BlueRibbon;

public enum TabModeKindType
{
    single
};
public enum SearchModeKindType
{
    ribbonType
};

public enum RibbonKindType
{
    NOT_RECORD,
    NOT,
    RIBBON_ONE,
    RIBBON_TWO,
    RIBBON_THREE,
    ATTENTION,
    NEW
}